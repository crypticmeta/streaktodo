import { NotificationsScreen } from '../../src/screens/NotificationsScreen';

// V0 placeholder: the full Profile/Settings surface arrives in Phase 10.
// For now Profile hosts the notification primitive test surface and sign-out
// (already implemented in NotificationsScreen).
export default function ProfileScreen() {
  return <NotificationsScreen />;
}
